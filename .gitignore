# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# SketchUp backups
*.skb

# Build output — the .rbz is published only on GitHub Releases, never committed
/dist/
/tmp/
*.tmp

# Changelog copied into the plugin at build time (source of truth is root)
/skp_e_plumb/CHANGELOG.md
